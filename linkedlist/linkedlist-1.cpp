// linkedlist definition
// a seq of data structures connected to each other through lists
// a list of links. each link has a data part and a ref (pointer) to the next list
// last node, points to null. link is ended.
